# Test configuration for integration tests
server:
  ip: "127.0.0.1"
  port: 0  # Use port 0 for random assignment in tests
databaseconfig:
  host: "localhost"
  port: 5432
  username: "postgres"
  password: "password"
  database_name: "rusty_word_smith_org"
  require_ssl: false
templateconfig:
  dir: test_dir/templates
  pattern: "**/*"
  extra_templates: []
staticserverconfig:
  mount_path: "/static"
  root_dir: "test_dir/static"
  serve_index: false 